import { createContext } from "react";

const ThemeContext = createContext({
  // theme: {"light", setTheme},
  // switchBtn: [false, setSwitchBtn],
  // setTheme: () => {},
  // setSwitchBtn: () => {},
});

export default ThemeContext;
